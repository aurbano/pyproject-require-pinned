[tool.poetry.dependencies]
python = ">=3.9.0"
something = "1.0.0"
array-form = [{ version = "1.2.3" }, { version = "0.1.2" }]
local-dep = { path = "some/path", develop = true }

[tool.poetry.group.dev.dependencies]
something = "1.0.0"
